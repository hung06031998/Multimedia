b1-a
các biến:
f : tần sô hàm sin
fs: tần số lấy mẫu
A:biên độ cơ bản của hàm sin
N:số chu kỳ cần lấy mẫu
T:chu kì
//các biến được set sẵn giá trị,sửa lại trực tiếp trong file.
a = số mẫu cần lấy tính theo fs (đây là tham số cho hàm linspace)

b1-b
f : tần sô hàm sin
fs: tần số lấy mẫu
A:biên độ cơ bản của hàm sin
N:số chu kỳ cần lấy mẫu
n:tham số cho hàm S
T:chu kì
//các biến được set sẵn giá trị,sửa lại trực tiếp trong file.
a = số mẫu cần lấy tính theo fs (đây là tham số cho hàm linspace)


//chạy bằng spyder